export interface Authentication {
    user: {
        email: string;
        password: string;
    };
}
export interface Registration {
    user: {
        username: string;
        email: string;
        password: string;
    };
}
